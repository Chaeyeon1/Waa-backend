name: deploy

on:
  push:
    branches:
      - main

jobs:
  AUTO_DEPLOY:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Run scripts in server
        uses: appleboy/ssh-action@master
        with:
          password: ${{ secrets.PASSWORD }}
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          port: ${{ secrets.PORT }}
          script: |
            export DATABASE_URL=${{ secrets.DATABASE_URL }}
            export SECRET_KEY=${{ secrets.SECRET_KEY }}
            export SECRET_KEY=${{ secrets.TWILIO_ACCOUNT_SID }}
            export SECRET_KEY=${{ secrets.TWILIO_AUTH_TOKEN }}
            export SECRET_KEY=${{ secrets.MY_SERVICE_NUMBER }}
            export SECRET_KEY=${{ secrets.EMAILADDRESS }}
            export SECRET_KEY=${{ secrets.EMAILPASSWORD }}
            sudo apt update
            sudo apt install git
            curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.38.0/install.sh | bash
            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
            nvm install node
            npm install -g pm2
            cd Waa-backend
            npm install
            ls
            pm2 kill
            git pull origin main
            npm run start
